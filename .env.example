# --- Core Platform ---
# Filled locally; CI may override DATABASE_URL
DATABASE_URL="postgresql://user:password@localhost:5432/lionstour"

# Mapbox
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=""

# Supabase
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
# Server-side service key (never expose to client!)
# Used for server reindexing of RAG into Supabase
SUPABASE_SERVICE_ROLE=""

# Stripe
STRIPE_SECRET_KEY=""

# AI (choose one)
# OpenAI direct (recommended for local dev)
OPENAI_API_KEY=""
# Or Vercel AI Gateway key
# AI_GATEWAY_API_KEY=""

# Admin API token for protected admin routes (server-side enforcement)
# If set, moderation APIs require header: Authorization: Bearer $ADMIN_API_TOKEN
ADMIN_API_TOKEN=""
